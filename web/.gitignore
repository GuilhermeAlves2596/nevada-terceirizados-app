# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (a config pública fica em src/lib/firebase.ts; use .env só p/ override)
.env*.local
# … mas o exemplo (só documentação de overrides) é versionado. O .gitignore da
# raiz tem `.env.*`, então re-incluímos explicitamente aqui:
!.env.local.example

# typescript
next-env.d.ts
*.tsbuildinfo
