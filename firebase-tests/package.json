{
  "name": "nevada-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Testes automatizados das Firebase Security Rules (Fase 12) via emulador.",
  "scripts": {
    "test": "node --test",
    "emulate": "firebase emulators:exec --config ../firebase.json --project demo-nevada --only firestore,storage \"node --test\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.13.2"
  }
}
